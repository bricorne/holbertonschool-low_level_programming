#include <stdio.h>
#include <stdlib.h>
#include <ctype.h>
#include "main.h"
/**
 * _abs - computes the absolute value of an integer
 * @n: An input character
 * Return: always 0.
 */

int _abs(int n)
{
abs(n);
return (0);
}
