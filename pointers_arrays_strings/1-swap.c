#include <stdio.h>
#include <stdlib.h>
#include <ctype.h>
#include "main.h"
/**
 * swap_int - Check Holberton
 *@a: an input
 *@b: an input
 * Return: 1 if is lowercase or 0 if is uppercase
 */

void swap_int(int *a, int *b)
{
temp = *a
*a = *b;
*b = temp;
}
