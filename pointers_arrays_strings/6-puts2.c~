#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include "main.h"
/**
 * rev_string - Check Holberton
 *@s: an input
 * Return: 1 if is lowercase or 0 if is uppercase
 */

void puts2(char *str)
{
  printf("%c\n", str);
}
