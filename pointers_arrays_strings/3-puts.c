#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include "main.h"
/**
 * _puts - Check Holberton
 *@str: an input
 * Return: 1 if is lowercase or 0 if is uppercase
 */

void _puts(char *str)
{
char x = puts(str);
printf("%d \n", x);
} 
