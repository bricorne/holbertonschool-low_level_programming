#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include "main.h"
/**
 * _strlen - Check Holberton
 *@s: an input
 * Return: 1 if is lowercase or 0 if is uppercase
 */

int _strlen(char *s)
{
strlen(s);
}
